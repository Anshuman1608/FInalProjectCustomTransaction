import { CustomTransactions } from './custom-transactions';

describe('CustomTransactions', () => {
  it('should create an instance', () => {
    expect(new CustomTransactions()).toBeTruthy();
  });
});
